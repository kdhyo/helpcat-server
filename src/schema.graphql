type Query {
  userAll: [User]
  user: User!
  hello(name: String): String
}

type Mutation {
  signup(
    userid: ID!
    password: String!
    userName: String!
    nickName: String!
    gender: String!
    phone: Int!
    address: String!
    email: String!
    birh: String!
  ): res!
}

type User {
  id: ID!
  userid: String
  userName: String
  nickName: String
  gender: String
  phone: Int
  address: String
  email: String
  birh: String
}

type AuthPayload {
  token: String
  user: User
}

type res {
  code: Int!
  result: User
  message: String!
}
